problem link : https://www.geeksforgeeks.org/problems/implementing-floyd-warshall2042/1?utm_source=youtube&utm_medium=collab_striver_ytdescription&utm_campaign=implementing-floyd-warshall

class Solution:
	def shortest_distance(self, matrix):
	    n, m = len(matrix), len(matrix[0])
	    
	    for via in range(n):
    		for i in range(n):
    		    for j in range(m):
    		        if matrix[i][j] == -1:
    		            matrix[i][j] = float('inf')
    		            
		            matrix[i][j] = min(matrix[i][j], matrix[i][via] + matrix[via][j])
            
            if via == n-1:
                for i in range(n):
                    for j in range(m):
                        if matrix[i][j] == float('inf'):
                            matrix[i][j] = -1 
                
        return matrix
        
''' time complexity : O(n ^ 3)
    sapce complexity : O(1)       using input matrix for result evaluation  
'''
